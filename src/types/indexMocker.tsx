import {initUserMock} from '../userMocker';
import {initTransactionMock} from '../transactionMocker';

// Initialize user mocks
initUserMock(true); // Enable user mocks

// Initialize transaction mocks
initTransactionMock(true); // Enable transaction mocks
